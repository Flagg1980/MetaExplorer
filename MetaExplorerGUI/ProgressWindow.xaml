<Window x:Class="MetaExplorerGUI.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Progress" Height="170" Width="500" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="None" BorderThickness="2" BorderBrush="Black">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="20" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <Image Grid.Column="0" Grid.Row="0" Source="/Resources/HardDiskImage.png" HorizontalAlignment="Left"/>

        <TextBlock Name="HeadingTextBlock" Grid.Row="0"  Text="" FontFamily="Tahoma" FontSize="20" Margin="125,10,0,0" Grid.ColumnSpan="2"/>

        <ProgressBar Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Name="MyProgressBar" Height="20" VerticalAlignment="Center" Margin="10,0,10,0"/>

        <TextBlock Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" Text="Processing File:" VerticalAlignment="Center" Name="MyProgressTextblock" Margin="10,0,10,0"/>

    </Grid>
</Window>
